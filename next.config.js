/** @type {import('next').NextConfig} */
const nextConfig = {
     images: {
          domains: ['localhost', 'assets.aceternity.com', 'upload.wikimedia.org', 'miro.medium.com', 'images.unsplash.com', 'api.microlink.io', 'aceternity.com'],
     }
}

module.exports = nextConfig
